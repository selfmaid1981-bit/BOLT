# CreatorFlow (BOLT)

UGC affiliate “game plan” app by **Michelle Corrigan** (Playhard / Self-Maid): niche picker, 4-step roadmap, income math, and a mini script studio (hook → proof → payoff).

```bash
npm install
npm run dev
```

Open `http://localhost:5173`. Sample earnings on the hero are labeled **Demo data** for walkthroughs / REWORK proof videos.
